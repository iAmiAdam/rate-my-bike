<% provide(:title, @user.username) %>

<div class="container-fluid">
	<div class="row">
			<div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-2 col-md-offset-2 col-lg-2 col-lg-offset-2">
				<div class="well">
					<%= render 'shared/mini_profile' %>
				</div>
				<div class="well">
					<%= render 'shared/user_stats' %>
				</div>
			</div>
		<div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-6 col-md-offset-0 col-lg-6">
			<div class="well">
				<%= render 'shared/user_bikes' %>
			</div>
			<div class="well">
				<%= render 'shared/user_comments' %>
			</div>
		</div>
	</div>
</div>
